import minif2f_import

open_locale big_operators
open_locale real
open_locale nat
open_locale topological_space

theorem mathd_algebra_96
  (x y z a : ℝ)
  (h₀ : 0 < x ∧ 0 < y ∧ 0 < z)
  (h₁ : real.log x - real.log y = a)
  (h₂ : real.log y - real.log z = 15)
  (h₃ : real.log z - real.log x = -7) :
  a = -8 :=
begin

  let z : ℝ := real.log x,
  let y : ℝ := real.log y,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let y : ℝ := real.log y,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let y : ℝ := real.log y,
  let a : ℝ := real.log x - real.log y,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let y : ℝ := real.log y,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let a : ℝ := real.log y - real.log z,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let y : ℝ := real.log y,
  let a : ℝ := real.log z - real.log x,
  let x : ℝ := real.log x,
  let z : ℝ := real.log z,
  let y : ℝ := real.log y,
  let a : ℝ := real.log x - real.log z,
  let x : ℝ := real.log x,

end
